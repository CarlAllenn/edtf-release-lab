[package]
name = "lab-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Fixture binary crate for the org release pipeline"

[dependencies]
lab-core = { workspace = true }
# A real C dependency, deliberately: it proves the musl C toolchain in the
# shared rust-binary workflow is load-bearing, the way ring and mimalloc
# do in iiif-server's production lock.
mimalloc = "0.1"
